import { motion } from "framer-motion";
import { Activity, BarChart3, Filter } from "lucide-react";
import { cn } from "@/lib/utils";
import type { Page } from "@/App";
const navItems: Array<{ key: Page; label: string; icon: typeof Activity }> = [{ key: "rating", label: "Рейтинг", icon: Activity }, { key: "selection", label: "Отбор моделей", icon: Filter }, { key: "statistics", label: "Статистика", icon: BarChart3 }];
export function Navbar({ current, onNavigate }: { current: Page; onNavigate: (page: Page) => void }) { return <header className="fixed top-0 left-0 right-0 z-40 flex justify-center px-4 pt-4"><motion.nav initial={{ y: -20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} className="glass-surface rounded-full px-2 py-1.5 flex items-center gap-1 shadow-ambient" aria-label="Навигация">{navItems.map((item) => { const Icon = item.icon; return <button key={item.key} onClick={() => onNavigate(item.key)} className={cn("relative inline-flex items-center gap-1.5 px-3 py-1.5 text-xs font-medium rounded-full transition-colors", current === item.key ? "text-white" : "text-ink-400 hover:text-ink-200")}><Icon className="h-3.5 w-3.5" />{current === item.key && <motion.span layoutId="nav-pill" className="absolute inset-0 rounded-full bg-accent" />}<span className="relative z-10">{item.label}</span></button>; })}</motion.nav></header>; }
